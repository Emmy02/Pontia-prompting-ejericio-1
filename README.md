# Configuraciones generales.

Este proyecto está utilizando GEMINI como LLM.

Para hacer que funcione es necesario crear una variable de entorno en el archivo .env con la siguiente key:
GEMINI_API_KEY = tu_api_key